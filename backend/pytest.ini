[pytest]
asyncio_mode = auto
timeout = 30
markers =
    ai: tests requiring GPU/Ollama (deselect with '-m "not ai"')
    production: tests running against production URL
    performance: load and performance tests
testpaths = tests
